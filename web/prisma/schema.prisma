generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id            String         @id @default(uuid())
  email         String         @unique
  passwordHash  String
  role          String         @default("user")
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt
  passwordResets PasswordReset[]
  notifications Notification[]
}

model PasswordReset {
  id        String   @id @default(uuid())
  token     String   @unique
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  expiresAt DateTime
  createdAt DateTime @default(now())
}

model Application {
  id        String   @id @default(uuid())
  name      String
  role      String
  location  String?
  exp       String?
  edu       String?
  industry  String?
  budget    String?
  photo     String?
  score     Int      @default(0)
  status    String   @default("pending")
  visaStatus String?
  resumeUrl String?
  skills    Json?    // Array of skills [Comm, Tech, Lead, Exp, Edu]
  timeline  Json?    // Array of timeline objects
  documents Document[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Job {
  id               String   @id @default(uuid())
  title            String
  country          String
  salary           String?
  category         String
  status           String   @default("active")
  responsibilities String?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
}

model Notification {
  id        String   @id @default(uuid())
  message   String
  isRead    Boolean  @default(false)
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())
}

model Document {
  id            String      @id @default(uuid())
  candidateId   String
  application   Application @relation(fields: [candidateId], references: [id], onDelete: Cascade)
  documentType  String
  uniqueCode    String      @unique
  pdfUrl        String
  isPaid        Boolean     @default(false)
  paymentStatus   String      @default("pending")
  paymentProvider String?
  transactionId   String?
  createdAt       DateTime    @default(now())
  updatedAt       DateTime    @updatedAt
}

model Settings {
  id              String   @id @default("global")
  receiveAlerts   Boolean  @default(true)
  defaultCurrency String   @default("USD")
  publicForms     Boolean  @default(true)
  soundAlerts     Boolean  @default(true)
  updatedAt       DateTime @updatedAt
}

model PageContent {
  id        String   @id @default(uuid())
  slug      String   @unique
  title     String
  content   String   @db.Text
  updatedAt DateTime @updatedAt
}
